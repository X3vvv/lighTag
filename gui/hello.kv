#:kivy 2.1.0
<MyLayout>
    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height
        spacing: 6
        padding: 10

        Image:
            source: 'gui\lighTag\imgs\settings-black.png'

            # below 2 lines can set the image to fill the window
            # allow_stretch: True
            # keep_ratio: False

        Button:
            text: "Submit"


